<div class="mb-sm">
<%= question.body %> 
</div>
<div class="text-sm question-footer">
<div class="text-grey">
<%= l(question.created_at, format: :short) %> 
<p>From: <%= "@#{get_user_by_id(question.from_user_id).first}" %></p>
</div>
<div>
  <%= link_to fa_icon("reply 2x"), new_question_answers_path(question.id), title: "Reply question" %>
</div>
</div>
